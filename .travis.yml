language: python
python:
  - "3.6"
install:
  - pip install -e .[dev]
  - python setup.py sdist
script:
  - python setup.py test

deploy:
  provider: pypi
  user: $PYPI_USER
  password:
    secure: $PYPI_PASSWORD
  on:
    branch: master
